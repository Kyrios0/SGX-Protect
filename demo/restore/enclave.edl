enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* define ECALLs here. */
		public void hello_sgx([out, size=12]char *des);
		public void give_me_pointer([in, size=funcLen]unsigned char *sFunc, int funcLen);
    };

    untrusted {
        /* define OCALLs here. */
		void unsafe_printf([in, size=20]char* str);
    };
};
